/** Local image assets referenced across the app. */
export const images = {
  /** Marker on the daily goal progress bar. */
  progressMarker: require('@/assets/images/blueberry_image.png'),
} as const;
