/** Local image assets referenced across the app. */
export const images = {
  /** Marker on the daily goal progress bar. */
  pomegranateMarker: require('@/assets/images/pomegranate_image.png'),
} as const;
