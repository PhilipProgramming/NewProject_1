/** Local image assets referenced across the app. */
export const images = {
  /** Marker on the daily goal progress bar. */
  pomegranateMarker: require('@/assets/images/pomegranate.png'),
} as const;
